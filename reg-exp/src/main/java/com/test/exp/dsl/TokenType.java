package com.test.exp.dsl;

public enum TokenType {
    IDENTIFIER, TYPE, NUMBER, STRING,
    COLON, EQUAL, COMMA,
    LBRACKET, RBRACKET, LBRACE, RBRACE,
    DOLLAR_REF, PLUS, MINUS, MULT, DIV,
    EOF
}
